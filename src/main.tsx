import React from "react";
import ReactDOM from "react-dom/client";
import { MelissaApp } from "./MelissaApp";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <MelissaApp />
  </React.StrictMode>
);
